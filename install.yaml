# Default settings
- defaults:
    link:
      relink: true

# Check home for dead symlinks and delete them
- clean: ['~']

- includes:
    shell: shell.yaml
    tmux: tmux.yaml
    zsh: zsh.yaml
    easyeffects: easyeffects.yaml
    wireplumber: wireplumber.yaml
    kitty: kitty.yaml
